// @ts-check

/** @template T @typedef {import('vue').Plugin<T>} Plugin */

/**
 * @typedef {unknown} PosStorePluginOptions
 */

export const posStorePlugin = /** @type {Plugin<PosStorePluginOptions>} */ ({
  install(app) {
  },
});
